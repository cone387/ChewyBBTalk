# 验收记录

日期：2026-09-07。

- Web 单元测试 13 项通过；新增上传部分失败与重试、重复重试保护、移除/重置后的迟到结果、卸载中止测试。
- Playwright 全量 16 项通过（桌面和小屏各 8 项，1.4 分钟），包含上一轮核心流程 6 项及本轮新增 10 项。
- 新增浏览器回归：真实附件上传及刷新持久化；注入 413、网络中断和 500，验证新建、更新、防窥发布失败保留；失败项移除后可再次选择相同文件。
- UI：已查看上传失败、375px 竖屏、812px 横屏及大字体弹窗截图。长文本可滚动、附件移除按钮可直接触摸，弹窗选项纵向排列、内容可滚动到操作按钮，Escape 可关闭。
- ESLint、TypeScript、Vite 生产构建、OpenSpec 严格校验和 git diff --check 通过。构建仍提示现有 Browserslist 数据较旧。

## 验证边界

- 大字体为根字号 24px；启用深色系统偏好与减少动态效果。Web 当前只实现浅色界面，本轮没有新增深色主题。原生端有独立主题系统，本轮未修改或在真机验证。
- 没有真机键盘、语音、后台恢复、WebKit/Firefox 或生产 Nginx 验收结论。
- 上传成功及发布更新通过临时数据库的真实 Django API；导入弹窗只使用验证结果 fixture 展示，未执行导入。
- 文件重试状态保留在当前编辑器内存，刷新/导航后需要重选。移除会中止客户端上传，但不承诺服务端已接收文件的回滚。发布不自动重试，避免在缺少幂等契约时重复创建。
- 截图与报告在 frontend/test-results 和 frontend/playwright-report；CI 上传并保留 7 天。远端 CI/部署以对应提交的 Actions 状态为准。
