## 1. 提交与编辑协议

- [x] 1.1 后端持久回执、唯一约束、结果查询与并发/回滚/删除/隔离测试。
- [x] 1.2 后端条件更新与冲突测试，记录兼容协议。
- [x] 1.3 Web 持久提交、结果不确定恢复、编辑冲突处理与真实响应丢失回归。
- [ ] 1.4 原生与桌面接入持久提交及结果核对，验证重启与身份隔离。

## 2. 多端刷新

- [x] 2.1 Web 返回前台和网络恢复刷新，保留筛选/编辑，拒绝旧响应。
- [ ] 2.2 原生前台刷新和编辑冲突，桌面唤醒会话/提交状态核对，补关键测试。

## 3. 反馈与兼容性

- [x] 3.1 统一 Web 记录/评论/标签的确认、错误和重试组件，键盘及取消行为验证。
- [x] 3.2 补 Firefox/WebKit 关键流程，完成三浏览器回归和界面走查。

## 4. 部署与维护

- [x] 4.1 随机或显式初始化密码，注册开关和认证限流，兼容现有账号并更新配置说明。
- [x] 4.2 实现有权限边界的服务/存储/备份状态 API 和 Web 入口，验证错误与信息隔离。

## 5. 交付

- [ ] 5.1 对齐 README、ROADMAP 和本轮完整验收记录，完成各端检查。
- [ ] 5.2 分轮提交推送并验证最终 CI、镜像部署及运行状态。

后端协议阶段：9 项新增测试覆盖并发创建/编辑、事务回滚、删除不复活、账号隔离、载荷冲突、CORS 和旧客户端兼容；后端全量 73 项通过，makemigrations --check --dry-run 无遗漏。三端接入与其他任务继续跟进。

Web 协议接入：持久提交与草稿版本事务核对、原提交核对/重试、冲突比较后条件保存已实现。新增 submissions 浏览器用例与草稿关联用例合计 20 项通过；构建、lint 和 26 项单元测试通过。本地首次全量运行发生 Vite 退出及上传等待超时，保留终端重新执行后 50 项全量通过（3.9 分钟）；交付状态单独跟进。

移动端阶段：AsyncStorage 串行保存原提交并按服务器/账号隔离，Compose 接入核对/原键重试、条件编辑与冲突确认；成功清理失败保留回执。207 项移动端测试与 TypeScript 检查通过，新增 6 项验证持久存储、JS 重启、请求头及附件载荷重放。现有 Compose 测试是状态逻辑模拟，不作为界面集成验收；桌面接入、移动端界面集成验证及前台刷新未完成，1.4/2.2 保持未勾选。

桌面阶段：主进程落盘提交键与完整请求载荷，IPC 接入发布、核对、重试及确认清理；编辑器 focus/online 时核对会话与待确认结果；草稿改为服务器/用户作用域，异步上传拒绝跨身份加入。登录/续期增加会话代次保护。25 项桌面测试、主进程与渲染进程类型检查、构建通过。真实窗口集成和恢复提示布局仍待验收，1.4/2.2 保持未完成。

前台刷新阶段：Web focus/visibility/online 按当前筛选刷新并合并重复事件；列表请求标识拒绝乱序响应，乐观删除隐藏标识保护撤销期间刷新。移动端增加 Home focus/AppState/网络恢复刷新，并修复下拉刷新与分页丢失搜索词。Web 29 项单元测试、lint/构建，移动端 210 项测试与类型检查通过。桌面/小屏浏览器已验证前台刷新、网络恢复保留筛选和输入；发现并修复原提交重试前未确保新输入落盘的问题。移动端实际界面与桌面窗口集成仍待验收。

Web 反馈与兼容验收：统一评论确认/错误/重试，记录删除、标签排序和复制失败反馈；重试原评论不清除后来的输入。Modal 顶层挂载修复小屏导航盖住遮罩，网络错误中文化。31 项单元测试、构建和 lint 通过；Chromium 桌面/小屏全量以及 Firefox/WebKit 关键流程合计 74 项通过（7.2 分钟）。已走查四配置评论重试截图，CI 安装与运行三引擎。

初始化凭据阶段：移除固定默认管理员密码，使用显式密码或受限目录中的随机凭据文件，密码不写日志，已有账号不轮换；演示账号默认不新建，可显式启用。配置与启动说明同步更新，75 项后端测试通过。注册策略与运行状态入口未完成，4.1/4.2 保持未勾选。

认证策略验收：默认关闭注册，三类可配置认证限流，公开策略 API 与 Web 入口接入，配置说明记录代理及多进程边界。79 项后端测试、31 项 Web 单元测试、构建和 lint 通过；新增两类登录/注册策略浏览器用例在四配置共 8 项通过。

运行状态验收：新增账号隔离的服务/当前存储/最近备份检查，管理员额外检查数据库与附件磁盘。83 项后端测试通过，新增状态页面四配置 8 项通过，Web 全量 90 项通过（7.8 分钟）。Windows 首次全量中 Vite 退出，已保留日志，改为直接启动 Vite Node 入口后全量通过。
