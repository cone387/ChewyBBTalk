# 验收记录

日期：2026-09-07。

最终 Web 全量浏览器回归 20 项通过（1.7 分钟），涵盖前三轮核心流程及本轮新增用例。

- 单元测试 16 项通过，新增图片失败重试、自定义占位图、解码回调、内联回调稳定性、切换图片后的迟到请求隔离及 Object URL 释放。
- 新增浏览器用例在桌面和小屏共 4 项通过：真实上传图片，依次注入下载中断、损坏图片和有效 PNG，确认重试实际请求与解码；长链接、两类代码块及宽表格不撑破卡片，并实际检查局部 scrollLeft。
- 已查看 375px 图片失败状态和长内容截图；812px 横屏同样包含在布局回归中。
- ESLint、TypeScript、Vite 生产构建、OpenSpec 严格校验和 git diff --check 通过。现有 Browserslist 数据过旧提示仍在。

## 边界

图片响应为测试定向注入，不能推断生产媒体服务可用性。一次复测中 Vite 测试服务中途退出（ECONNREFUSED），重新启动独立测试环境后新增 4 项通过。没有新增 Web 深色主题、PWA 或阅读/编辑入口变化；原生真机与其他浏览器引擎仍需另行验收。

上一提交 59da86a 的远端 CI 通过，镜像构建通过，但服务器自动部署在拉取 ChewyAttachment 时发生 TLS 中断；本轮提交的 CI/部署以对应 Actions 结果为准。
