## 1. 持久化与隔离

- [x] 1.1 实现 IndexedDB 草稿存储、账号服务器记录键及并发版本检查。
- [x] 1.2 实现自动保存、加载、卸载提交及离开提醒，明确存储失败。

## 2. 编辑器接入

- [x] 2.1 恢复正文、标签、可见性、位置及附件；未完成文件支持手动重试。
- [x] 2.2 展示保存状态、恢复提示和清除确认；发布成功清理，失败保留。

## 3. 验证与交付

- [x] 3.1 验证刷新、导航、账号及记录隔离、失败文件恢复、并发冲突和清除竞态。
- [x] 3.2 完成 Web 检查和浏览器回归，更新路线图，提交推送并跟进 CI。

## 验证记录

2026-09-07：22 项 Vitest、34 项 Playwright（桌面与 375px 小屏）通过，lint、TypeScript 与生产构建通过。浏览器回归包含真实 IndexedDB、真实 Django 上传后下载核对文件字节、发布失败后刷新、成功清除、导航/身份/记录隔离、并发清除保护、配额失败及原记录更新提示。已走查恢复草稿的桌面与小屏截图。提交后 CI 状态另行跟进。

提交 75a5032，随 a7cbe27 推送 master；CI run 34093149486 全部通过。部署验收独立跟进，不以 CI 成功代替线上检查。
