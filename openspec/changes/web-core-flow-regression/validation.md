# 验收记录

本地验证日期：2026-09-07。

- Web 单元测试：10 项通过，包含连续删除、撤销截止时间、接口失败恢复和提示消失计时。
- Playwright：6 项通过（桌面 Chromium 与 375px 小屏 Chromium 各 3 项）。使用独立临时数据库和真实 Django API，覆盖错误密码后重试、发布、编辑、搜索、删除撤销、最终删除和连续删除；刷新后核对持久化结果。
- Web ESLint、TypeScript 与 Vite 生产构建通过。
- OpenSpec 严格校验及 git diff --check 通过。
- 已查看小屏截图，确认发布文字保持单行、操作入口可见，撤销提示位于底部导航上方；浏览器测试同时检查页面没有横向溢出。

回归使用 Vite 开发服务；生产构建单独验证。本轮不代表生产 Nginx、原生移动端、真机、WebKit 或 Firefox 已通过浏览器回归。远端 CI 与部署状态以本次提交对应的 GitHub Actions 结果为准，本地验证不等同于部署成功。

执行方式及隔离机制见 frontend/e2e/README.md。CI 保存浏览器报告、失败追踪及截图 7 天。
