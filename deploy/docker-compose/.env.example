# ================================
# Docker Compose 多容器部署环境变量
# ================================

# Django 密钥（生产环境必须修改为随机字符串）
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# 数据库连接
# SQLite（默认）
DATABASE_URL=sqlite:///./db.sqlite3

# PostgreSQL（推荐生产环境使用）
# DATABASE_URL=postgresql://chewybbtalk:password@postgres:5432/chewybbtalk

# 七牛云存储配置（可选）
QINIU_ACCESS_KEY=
QINIU_SECRET_KEY=
QINIU_BUCKET_NAME=
QINIU_BUCKET_DOMAIN=

# Authelia 配置（生产环境必须修改）
AUTHELIA_SESSION_SECRET=change-this-to-a-random-string-at-least-32-chars
AUTHELIA_ENCRYPTION_KEY=you_must_generate_a_random_string_of_more_than_twenty_chars_and_configure_this

# 时区
TZ=Asia/Shanghai
