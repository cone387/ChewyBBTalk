# ================================
# ChewyBBTalk 环境变量配置
# ================================
# 复制为 .env 使用

# ================================
# 端口配置
# ================================
# 前端端口
FRONTEND_PORT=4010
# 后端端口
BACKEND_PORT=8020

# ================================
# Django 后端配置
# ================================
DEBUG=false
SECRET_KEY=change-this-to-a-random-secret-key-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# 数据库配置
# SQLite (开发环境)
DATABASE_URL=sqlite:////app/data/db/db.sqlite3
# PostgreSQL (生产环境推荐)
# DATABASE_URL=postgresql://username:password@localhost:5432/chewybbtalk
# MySQL (可选)
# DATABASE_URL=mysql://username:password@localhost:3306/chewybbtalk

# 媒体文件配置
MEDIA_ROOT=/app/data/media
STATIC_ROOT=/app/data/staticfiles

# CORS 配置
CORS_ALLOWED_ORIGINS=http://localhost:4010,https://your-domain.com
CORS_ORIGIN_ALLOW_ALL=False

# ================================
# 系统默认账号配置
# ================================
# 系统管理员账号（首次启动时创建）
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-this-password

# ================================
# S3 存储配置 (可选)
# ================================
# 如果配置了以下三个必填项，系统将自动使用 S3 存储
# 否则使用本地文件存储

# AWS S3 或兼容 S3 的服务凭证 (必填)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here
# AWS_STORAGE_BUCKET_NAME=your-bucket-name

# AWS S3 区域 (可选，默认 us-east-1)
# AWS_S3_REGION_NAME=us-east-1

# S3 兼容服务端点 (可选，用于 MinIO、阿里云 OSS 等)
# MinIO 示例: http://localhost:9000
# 阿里云 OSS 示例: https://oss-cn-hangzhou.aliyuncs.com
# AWS_S3_ENDPOINT_URL=

# 自定义域名 (可选，用于 CloudFront CDN 或自定义域名)
# AWS_S3_CUSTOM_DOMAIN=your-cloudfront-domain.cloudfront.net

# 文件默认访问权限 (可选，默认 private)
# AWS_DEFAULT_ACL=private

# 签名 URL 过期时间，单位秒 (可选，默认 3600)
# AWS_QUERYSTRING_EXPIRE=3600

# 附件最大文件大小，单位字节 (可选，默认 10MB)
# ATTACHMENT_MAX_FILE_SIZE=10485760

# ================================
# Docker 配置
# ================================
DATA_DIR=./data
