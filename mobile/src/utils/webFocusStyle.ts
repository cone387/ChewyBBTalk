/** Keyboard focus style injected by the Expo Web entrypoint. */
export const WEB_FOCUS_CSS = `
  input:focus-visible,
  textarea:focus-visible,
  select:focus-visible,
  [contenteditable]:focus-visible {
    outline: 2px solid #2563eb !important;
    outline-offset: 2px;
  }
`;
